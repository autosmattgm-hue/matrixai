{
  "name": "matrix-voice-os",
  "version": "1.0.0",
  "description": "Matrix, a cinematic voice-first AI operating shell powered by AI-head.png.",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/matrix-api.js && node --check api/health.js && node --check api/matrix/respond.js && node --check api/matrix/transcribe.js && node --check api/runtime-config.js && node --check public/app.js && node --check public/sw.js && node --check public/runtime-config-fallback.js",
    "vercel:dev": "vercel dev",
    "vercel:deploy": "vercel",
    "vercel:prod": "vercel --prod"
  },
  "dependencies": {}
}
